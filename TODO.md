# 実装計画

今すぐに実装して欲しい訳ではないけど、将来的にこれらを見据えて作業をして欲しいことを列挙します。
「こうした方がいいのではないか」という意見があれば言って下さい。

* 備忘録
  * Plan 30 Phase 4を未実施のままPlan 31を行うので、後で行うこと。

* HAL
  * ターゲットはCardputer Adv, PC(Windows, macOS), Web (WASM)とします。
* 文字描画
  * 描画色を引数から指定できるようにする。
  * パフォーマンス向上のため、現在Pure Pythonで実装されている文字描画処理（`draw_text` / `fb.text()`）をCモジュール側に移植し、`push_draw_text` として提供する。
  * 影付き文字などの重ね描きパターンはあらかじめアトラス画像側に合成・収録しておき、1回の `blt` で高速に描画できるようにする。
    * （フォント変換ツール側の拡張と、エンジン側でのスタイル/オフセット指定の仕組みを追加する）
* フォント
  * 用途に応じたフォントフォーマットの明示的な分離（AFN1とAFN8）を実装する。
    * AFN1: 影なし単色用（1ピクセル=1ビット）。高圧縮でtint機能と相性が良い。動的な影付け（text_shadowed）と併用。
    * AFN8: 影や縁取りなどのスタイル焼き込み済み用（1ピクセル=1バイト）。複数色を保持し、tintを使わず1回のbltで最速描画する用途。
* サウンド
  * Cardputer Advで可能なサウンド機能を付けたい。1音ぐらいなら行ける? (done)
  * MMLで音楽演奏も欲しい。 (done)
  * Cardputer Adv上で公式MicroPythonではサウンドICに直接アクセスできないのであれば、そこをブリッジするCモジュールを作成する。メンテを考慮して可能であれば薄い関数で。(done)
    * Cモジュール作成にはDockerを使って他のPCに移動させても開発を継続できるようにしたい。(done)
    * Cモジュールの動作確認ができたらM5モジュール利用部分は削除する。(done)
  * **サウンドシーケンサーのC言語化**
    * 現在Python側の `update()` で管理しているMML/音符のシーケンス進行処理を、C言語エンジン（バックグラウンドタスク）に移行する。これによりPython側のフレーム落ちによる再生タイミングのジッター（ヨレ）を完全に排除し、自律的に安定した演奏を行えるようにする。(done on CardputerAdv)
  * **WASM版のサウンドエンジン統一 (AudioWorklet化)**
    * 将来的に波形エディタなどを実装する際、PC版とWASM版で発音が異なる問題を解決するため、PC版と同じ `sound_engine.c` をWASMにコンパイルする。Web Audio APIの `AudioWorklet` を用いてC言語側が生成したPCM波形を直接再生し、全プラットフォームでバイトレベルで完全に同一の音を鳴らせるようにする。
* 入力
  * Cardputer Adv上の好きなキーをボタンに割り当てられる機能が欲しい。
  * PC側はキーとゲームコントローラに対応できると良い。(オプション)
  * **マウスおよびタッチパネル入力のサポート**:
    * 画面上の仮想ボタン（UI）をクリック・タップで操作できるようにするため、`mouse_x`, `mouse_y`, `mouse_button` などのポインティングデバイスの座標と状態を取得できるAPIをエンジンに追加する。
      * Cardputer Advではマウス・タッチパネル入力を無視する。

* テストの一時的なファイルは 「tests/tmp」 ディレクトリに置くようにして下さい。
* アサーション。リリース時に実行速度に悪影響を与えないようにしつつ、開発時には問題発覚の機会を増やしたい。
* パフォーマンス
  * ボトルネック箇所があればそれを特定する. プラットフォームごとに異なる可能性もある.
  * 恐らくFramebuffer.sprite()だが、その改善方法を検討する.
    * sprite(), blt()のC言語化を検討する. (done)
  * プロファイリングを実施する。 (done)
  * **オフスクリーン・レンダリング（Render-to-Texture）APIの提供**
    * 背景や大量の静的オブジェクトを一度別の `Framebuffer` に事前描画（`submit`時パレットなし）しておき、1枚の巨大な `Image`（スプライト）としてキャッシュ・再利用できる仕組みをPythonゲームレイヤーに公開する。これにより `motorcycle` のような背景負荷の高いゲームのFPSを劇的に向上できる。
    * ※ただし、フルスクリーン(240x135)のバッファを余分に確保すると約32KBのメモリを消費するため、メモリ枯渇への配慮が必要。全画面ではなく「更新頻度の低い空の部分だけ（240x60等）」など、部分的なオフスクリーンバッファを作れる柔軟なAPI設計を検討する。

# 内部設計

* 管理を意識した設計と実装
  * プラットフォーム間をまたいだコード
     * 可能であればプラットフォーム間でコード(Python, C)を共有する。(done)
* パフォーマンスと可用性の両立
  * エンジン機能はCore 1で、ゲームはCore 0で動かせるようにする。(done)
  * ゲーム側からそれを意識させないようなAPIであると嬉しいが、パフォーマンス上無理であればAPI変更もやむなしとします。
  * Cardputer Advのエンジン機能はファームウェアに実装することも検討します。(done)
  * **[Optional] Native C Module (.mpy) アーキテクチャの導入検討**
    * ファームウェアのビルドを回避しつつ高速なイテレーションを維持するため、ゲームロジック部分をC言語で記述し `mpy-cross / dynruntime` で `.mpy` バイナリとしてコンパイルして動的ロードする構成をオプションとして検討する（Pythonは薄いラッパーとして機能させる）。
  * **ヒープメモリ管理の最適化 (uPy Heapの圧迫解消)**
    * `DisplayList` などの巨大な構造体が現在MicroPythonヒープ(`m_new`)を圧迫している。コマンド最大キュー数を適正化（例: 4000 -> 1000）してサイズを縮小した上で、C言語標準の `malloc` (C Heap) に移行させ、ゲームロジック用のメモリ空間を解放する。(done)

# テストベッド

* このライトゲームエンジンの最初のターゲットとして ../motorcycle_cardputer/ を実装します。
  * 参照先は ../motorcycle_pyxel/ としますが、このディレクトリの中は書き換えないで下さい。
* motorcycle_cardputer の中身についても相談しながら実装を進めていきたいです。
* とは言え、この upy_light_engine ディレクトリの実装が当面の優先目標です。
