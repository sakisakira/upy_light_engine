# レビューおよび設計変更の履歴 (Review History)

このドキュメントは、コードレビューでの指摘事項と、それに基づく設計判断・変更履歴を記録するものです。

---

## 2026-06-14: HALクラス構造の分離と `blt` の汎用化

**【指摘内容】**
> 元々はスプライト(ARGB4444)とFramebuffer(RGB565)は別のクラスであると想定してたけど、同じクラスとしてパフォーマンス上問題なければそれでもいいです。その場合はフォーマットを外部から識別できるようプロパティなどを用意して、またbltの引数がどちらでも動くようにした方がいいと思います。microPythonの場合で実装する場合のことも考慮してパフォーマンスについては検討して下さい。
> 
> *(HALを使う側からは「Framebufer」という名前でアクセスできるといいと思います。)*

**【設計判断と対応】**
MicroPython環境におけるパフォーマンスとメモリ効率を考慮し、クラスを完全に分離する設計を採用しました。
- **`Image` クラスの新設**: ARGB4444フォーマットのスプライトデータを保持する専用クラス。MicroPython環境では `framebuf.FrameBuffer` を継承しないただのデータコンテナとすることで、初期化のオーバーヘッドとメモリ消費を最小限に抑止。
- **`Framebuffer` への名称変更**: `PyxelFrameBuffer` から名称を変更。RGB565の画面バッファを管理し、MicroPython環境では標準の `framebuf.FrameBuffer` を継承してネイティブの描画API(`rect`, `fill`等)を利用可能に。
- **`blt` の自動判定化**: 引数に渡されたオブジェクトの `.format` を確認し、`Image` (ARGB4444) であればアルファブレンド合成を、`Framebuffer` (RGB565) であれば `colkey` を使った透過コピーを自動的に行うように修正。

---

## 2026-06-14: アルファブレンド演算の高速化（ビットシフト化）

**【指摘内容】**
> aは0,15の場合はもう考慮しなくていいから、いっそのこと inv_a = 16 - a として // 16 を >> 4 としたら速くなることってない? もしそうなら検討してくれる? 速くならないのなら現状のままでいいよ。
> 
> *(対象コード: `out_r = (sr * a + dr * inv_a) // 15`)*

**【設計判断と対応】**
非常に有効な最適化テクニックであると判断し、実装に取り入れました。
- 完全に透明(`a=0`)と完全に不透明(`a=15`)のケースは事前に `continue` 等で分岐処理されているため、実際のブレンド処理では `a` は 1〜14 の値をとる。
- したがって、`15` での除算を `16` での除算で近似（`inv_a = 16 - a` とし、`>> 4` のビットシフトを使用）しても、各色成分の最大値スケールは崩れずに正しい結果が得られる。
- **効果**: マイコン環境（MicroPython + Viper）において、比較的重い「除算命令」を排除し、1サイクルの「ビットシフト命令」に置き換えることができたため、描画ループの大幅な高速化が実現した。

---

## 2026-06-14: Framebufferの `fill`, `rect` 追加と色フォーマットの統一

**【指摘・要望内容】**
> (TODO.mdより抜粋)
> * Framebuffer: ゲーム側に見せる色フォーマットはARGB4444とする。必要に応じて `fill_565` なども残しておく。
> * `fill`, `rect` を上の色フォーマットに従って実装する。

**【設計判断と対応】**
ユーザー向けAPIとして色指定を ARGB4444 に統一することで、利用側の負担を減らす設計としました。
- **内部変換メソッドの追加**: `_col4444_to_565(col)` を両HAL実装に追加し、ARGB4444の色情報を即座に内部バッファ用のRGB565に変換する処理を実装。
- **`fill`, `rect` の実装**: ARGB4444 で色を受け取り、内部で変換した上でバッファに書き込む（またはMicroPythonの場合は `super().fill` や `super().rect` に渡す）ように実装。
- **低レイヤーAPIの確保**: 生のRGB565値を直接渡せる `fill_565` と `rect_565` を用意し、パフォーマンスや互換性を重視する用途への逃げ道も確保した。

---

## 2026-06-14: 一般的なRGB(0-255)色指定ユーティリティの追加

**【指摘・要望内容】**
> ゲーム側に見せる色指定がARGB4444のままだと、利用者が直感的に色を指定しにくい懸念がある。ARGB8888(一般的なRGB)からARGB4444に変換するutility関数を用意してはどうか。

**【設計判断と対応】**
開発者の利便性を向上させるため、直感的な `0-255` ベースの色指定から内部フォーマットの整数を生成するヘルパー関数を実装しました。
- `hal_framebuffer.py` に `color(r, g, b, a=255)` 関数を追加。
- 内部でビットシフト (`>> 4` および `<<`) を行い、瞬時にARGB4444の整数値（例: `0xF00` など）を返す仕様とした。
- これにより、ゲームのロジック側では `hal.color(255, 0, 0)` のように自然に色を指定できるようになりつつ、HAL層の軽快な設計は維持される。

---

## 2026-06-14: HALモジュールのディレクトリ分割とPC上での自動テスト環境の構築

**【指摘・要望内容】**
> 将来的な追加（サウンド、ボタン入力等）を見据えた構成にしたい。また、自動テスト環境がないため追加したい。
> さらに、ルールに則り、コード中のDocstringおよびコメントは英語にする必要がある。

**【設計判断と対応】**
- **ディレクトリ構成の再編**:
  HAL関連のファイルを `hal/` パッケージ配下に移動し、`hal/framebuffer.py`, `hal/framebuffer_cpython.py`, `hal/framebuffer_micropython.py` に整理しました。これにより、将来的な `hal/sound.py` や `hal/input.py` の追加が容易になり、ルートディレクトリの肥大化を防ぎます。
- **自動テストとモックの導入**:
  MicroPython特有の `framebuf` や `@micropython.viper`、`ptr16` の呼び出しを含むコードをPC環境（CPython）で検証可能にするため、テスト実行時に専用のモック (`tests/mock_micropython.py`) を注入する機構を作成しました。これにより、標準の `unittest` を用いて、ロジック・文法の検証が可能になりました。
- **Docstringとコメントの英訳化**:
  ルールに基づき、`main.py` およびHALモジュール内のすべてのコメントとDocstringを英語に翻訳しました。

---

## 2026-06-14: 入力処理（Input）と描画プリミティブ（点・線）の追加

**【指摘・要望内容】**
> `motorcycle` の移植に向けて、入力処理（`btn`等）と描画機能（`pset`, `line`）が必要。斜め線の描画は使われていないため、垂直・水平線の軽量な描画に特化し、マウスは不要とする。またゲーム側からキーマッピングを変更可能にする。

**【設計判断と対応】**
- **入力モジュール (`hal/input.py`) の新設**:
  キー状態を管理する `btn` 関数を追加。CPython環境（`hal/input_cpython.py`）では Tkinter のキーイベントをフックし、`set_key_mapping` 関数によってゲーム側から動的にキーアサインを変更できる設計とした。
  Cardputerの実機に合わせて `GAMEPAD1_BUTTON_A` 等と `KEY_A` などを内部エイリアスとして同一に扱うことで、ゲーム側の移植負担を軽減した。
- **描画プリミティブ (`pset`, `line`) の実装**:
  `hal/framebuffer_cpython.py` および `_micropython.py` に `pset` と `line` を追加。
  ゲーム内で斜め線が使われていないという調査結果に基づき、`line` は `rect` (`hline`, `vline`) にフォールバックする軽量実装とした。CPython版では斜め線描画呼び出し時に `NotImplementedError` を送出して早期警告し、MicroPython環境では単に無視してクラッシュを防ぐ仕様とした。

---

## 2026-06-14: 入力関数の命名変更（`btn` -> `button`）

**【指摘内容】**
> 具体的には `btn` を `button` にして欲しい。

**【設計判断と対応】**
- **命名変更**: `hal/input_cpython.py`, `hal/input_micropython.py` の `btn` 関数を `button` にリネームしました。また、テストコード(`main.py`)側の呼び出し箇所も更新しました。

---

## 2026-06-14: カラーフォント変換ツールと描画機能の追加

**【指摘・要望内容】**
> BDFやTTF等を入力とし、太字や影付き等のエフェクトを焼き込んだカラー付きフォントを作れるツールが欲しい。プレビュー機能も付けたい。実機での効率を考慮し `.afnt` という拡張子を採用する。

**【設計判断と対応】**
- **変換ツール (`tools/font_converter.py`)**:
  Pillowを用いてTTFフォントからアスキー文字（0x20〜0x7E）を描画し、影や太字等のエフェクトを適用。結果を `preview.png` として出力しつつ、MicroPythonで最速ロード可能な ARGB4444 形式のバイナリファイル (`.afnt`) に変換して保存するツールを作成した。
- **テキスト描画API (`hal/font.py`)**:
  `.afnt` ファイルを読み込んで `hal.Image` オブジェクトとしてスプライトシート化する `Font` クラスを実装。`text` 関数により、指定座標へ `blt` を用いて一文字ずつ高速に切り出し描画する処理を追加した。

---

## 2026-06-14: PNGフォントのカラーリング対応とリファクタリング

**【指摘・要望内容】**
> フォントソースのPNGは字が黒だけど、ゲームで使うときには自由な色(今回は黄色など)に設定できるようにして欲しい（`convert_png`でcolorやshadow_color等を指定可能に）。プレビュー用の名前からは `_preview` を外し、元画像を `test_input_6px_font.png` にリネームしたい。

**【設計判断と対応】**
- **変換ツール (`tools/font_converter.py`) のアップデート**:
  - `convert_png` メソッドに `color`, `shadow_color`, `shadow_offset`, `bold` 引数を追加しました。
  - 元の透過PNGからアルファマスクを抽出し、Pillowの画像合成を用いて指定された色と影を動的に焼き付けてから `.afnt` を生成する処理にリファクタリングしました。
  - プレビュー画像の出力名を `_preview.png` ではなく元の拡張子置き換え(`.png`) に戻し、ツール単体で引数なし実行時にヘルプ（レイアウト仕様）が表示されるよう対応しました。
